# Welcome to Jekyll!
#
# This config file is meant for settings that affect your entire site, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# `jekyll serve -l -H localhost`. If you change this file, please restart the 
# server process.

# Basic Site Settings
locale                   : "en-US"
site_theme               : "air" # The available themes options are "default", "air", "sunrise", "mint", "dirt" and "contrast"
title                    : "Wendyellé A. Alban NYANTUDRE"
title_separator          : "-"
name                     : &name "Wendyellé A. Alban NYANTUDRE"
description              : &description "Machine Learning Engineer | Deep learning for vision, language and speech, with a focus on low-resource African languages"
url                      : https://anyantudre.github.io
baseurl                  : "" # the subpath of your site, e.g. "/blog"
repository               : "anyantudre/anyantudre.github.io"

# --- Giscus comments ---
# To enable: 1) Enable Discussions on your repo, 2) Install Giscus app, 3) Get IDs from giscus.app
giscus:
  repo: "anyantudre/anyantudre.github.io"
  repo_id: "R_kgDORF93gA"
  category: "Announcements"
  category_id: "DIC_kwDORF93gM4C1t7-"
  mapping: "pathname"
  reactions_enabled: "1"
  emit_metadata: "0"
  input_position: "bottom"
  theme: "dark"
  lang: "en"

# Site Author - The following control what appear as part of the author content on the side bar.
#               If a field is blank the icon and link will not appear, otherwise it will be shown.
#               Additional customization can be done by editing /_includes/author-profile.html
author:
  # Biographic information
  avatar           : "profile.jpg"
  name             : "Wendyellé A. Alban NYANTUDRE"
  pronouns         : "he/him"
  bio              : "Machine Learning Engineer | Deep learning for vision, language and speech | Looking for a PhD"
  location         : "Safi, Morocco"
  employer         : # no current employer — the GO AI Corporation role ended 08/2025
  uri              : # URL
  email            : "nyantudrealban@gmail.com" 

  # Academic websites
  academia         : # URL
  arxiv            : # URL
  googlescholar    : # URL
  inspire-hep      : # URL
  impactstory      : # URL
  orcid            : # URL
  semantic         : # URL
  ssrn             : # URL
  pubmed           : # URL
  researchgate     : # URL
  scopus           : # URL
  zotero           : # URL

  # Repositories and software development
  bitbucket        : # Username
  codepen          : # Username
  dribbble         : # Username
  github           : "anyantudre"
  kaggle           : "waalbannyantudre"
  stackoverflow    : # User number

  # Social media
  artstation       : # Username
  bluesky          : # Username
  facebook         : # Username
  flickr           : # Username
  foursquare       : # Username
  goodreads        : # Username
  google_plus      : # Username
  keybase          : # Username
  instagram        : # Username
  lastfm           : # Username
  linkedin         : "anyantudre"
  mastodon         : # URL
  medium           : # URL
  pinterest        : # Username
  soundcloud       : # Username
  steam            : # Username
  telegram         : # URL
  tumblr           : # Username
  twitter          : "anyantudre"
  vine             : # Username
  weibo            : # Username
  wikipedia        : # Username
  xing             : # Username
  youtube          : "anyantudre"
  zhihu            : # Username

# Publication Category - The following the list of publication categories and their headings
publication_category:
  books:
    title: 'Books'
  manuscripts:
    title: 'Journal Articles'    
  conferences:
    title: 'Conference Papers'

# Site Settings
teaser                   :  # filename of teaser fallback teaser image placed in /images/ for social media previews
breadcrumbs              : false # true, false (default)
words_per_minute         : 160
future                   : true
read_more                : "disabled" # if enabled, adds "Read more" links to excerpts
talkmap_link             : false      #change to true to add link to talkmap on talks page
comments:
  provider               : # false (default), "disqus", "discourse", "facebook", "google-plus", "staticman", "custom"
  disqus:
    shortname            :
  discourse:
    server               : # https://meta.discourse.org/t/embedding-discourse-comments-via-javascript/31963 , e.g.: meta.discourse.org
  facebook:
    appid                :
    num_posts            : # 5 (default)
    colorscheme          : # "light" (default), "dark"
staticman:
  allowedFields          : ['name', 'email', 'url', 'message']
  branch                 : "gh-pages" # "master", "gh-pages"
  commitMessage          : "New comment."
  filename               : comment-{@timestamp}
  format                 : "yml"
  moderation             : true
  path                   : "_data/comments/{options.slug}"
  requiredFields         : ['name', 'email', 'message']
  transforms:
    email                : "md5"
  generatedFields:
    date:
      type               : "date"
      options:
        format           : "iso8601" # "iso8601" (default), "timestamp-seconds", "timestamp-milliseconds"
atom_feed:
  hide                   : false     # change to true to hide the RSS feed in the footer
  path                   : # blank (default) uses feed.xml


# SEO Related
google_site_verification :
bing_site_verification   :
alexa_site_verification  :
yandex_site_verification :


# Social Sharing
twitter:
  username               : &twitter
facebook:
  username               :
  app_id                 :
  publisher              :
og_image                 :  # Social media default site image
og_description           :  # Social media default site image description
# For specifying social profiles
# - https://developers.google.com/structured-data/customize/social-profiles
social:
  type                   : # Person or Organization (defaults to Person)
  name                   : # If the user or organization name differs from the site's name
  links: # An array of links to social media profiles


# Analytics
analytics:
  provider               :  "false" # false (default), "google", "google-universal", "google-analytics-4", "custom"
  google:
    tracking_id          :


# Reading Files
include:
  - .htaccess
  - _pages
  - files
exclude:
  - "*.sublime-project"
  - "*.sublime-workspace"
  - .asset-cache
  - .bundle
  - .github
  - .jekyll-assets-cache
  - .sass-cache
  - assets/js/_main.js
  - assets/js/plugins
  - assets/js/vendor
  - CHANGELOG
  - Capfile
  - config
  - Dockerfile
  - Gemfile
  - Gruntfile.js
  - gulpfile.js
  - LICENSE
  - local
  - log
  - node_modules
  - package.json*
  - Rakefile
  - README
  - tmp
  - vendor
keep_files:
  - .git
  - .svn
encoding: "utf-8"
markdown_ext: "markdown,mkdown,mkdn,mkd,md"


# Conversion
markdown: kramdown
highlighter: rouge
lsi: false
excerpt_separator: "\n\n"
incremental: false


# Markdown Processing
kramdown:
  input: GFM
  hard_wrap: false
  auto_ids: true
  footnote_nr: 1
  entity_output: as_char
  toc_levels: 1..6
  smart_quotes: lsquo,rsquo,ldquo,rdquo
  enable_coderay: false
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: highlight
    block:
      line_numbers: true
      start_line: 1


# These settings control the types of collections used by the template
collections:
  teaching:
    output: true
    permalink: /:collection/:path/
  publications:
    output: true
    permalink: /:collection/:path/
  portfolio:
    output: true
    permalink: /:collection/:path/
  talks:
    output: true
    permalink: /:collection/:path/
  certifications:
    output: true
    permalink: /:collection/:path/
  awards:
    output: true
    permalink: /:collection/:path/
  # _news — ROUND 6. The home page's News section used to be four rows typed
  # into an include, linking to anchors on the page they already sat on. Each
  # milestone is now a real item with a real page of its own at /news/<slug>/,
  # so a row can be linked to from outside the site and can carry the detail
  # that never fitted on one line.
  news:
    output: true
    permalink: /news/:path/
  extra:
    output: true
    permalink: /:collection/:path/


# These settings control how pages and collections are included in the site
defaults:
  # _posts - Blog posts under /blog/
  - scope:
      path: ""
      type: posts
    values:
      layout: blog_post
      # `view` is what _layouts/redesign-default.html stamps on <html> as
      # data-view. A post is the design's `post` view: it marks Writing in the
      # navbar, widens the post's gutter to 4rem at 960px, gives it its own
      # left lane at 1300px, and tells assets/js/redesign.js what to spy on.
      view: post
      author_profile: true
      read_time: true
      comments: true
      share: true
      related: true
      toc: true
      permalink: /blog/posts/:title/
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: true
  # _teaching — INTEGRATION. A teaching entry and a talk are the same kind of
  # thing here: they sit interleaved on one /teaching-talks/ page, and every
  # title on it links through to one of these. They must therefore land on the
  # SAME detail page, so both collections share _layouts/talk.html, whose
  # rubric falls back from talk_type to this collection's `type`.
  - scope:
      path: ""
      type: teaching
    values:
      layout: talk
      view: talks
      author_profile: true
      share: true
      comments: true
  # _publications — the collection is empty, but were an entry to land in it
  # it would render on the redesign's shell like everything else.
  - scope:
      path: ""
      type: publications
    values:
      layout: redesign-legacy
      author_profile: true
      share: true
      comments: true
  # _portfolio — the redesigned project detail page. `view: work` keeps the
  # navbar marking Work while the reader is inside a project.
  - scope:
      path: ""
      type: portfolio
    values:
      layout: redesign-project
      view: work
      author_profile: true
      share: true
      comment: true
  # _talks
  - scope:
      path: ""
      type: talks
    values:
      layout: talk
      author_profile: true
      share: true
      # marks "Teaching" in the redesigned navbar: _layouts/redesign-default.html
      # writes data-view from page.view, and a layout's own front matter cannot
      # supply it, so the section a talk belongs to is declared here.
      view: talks
  # _certifications — INTEGRATION. Each one is a real page of prose that
  # /certifications/ and the CV both list; they were still rendering on the
  # Academic Pages `single` layout, i.e. the old masthead and the old footer,
  # one click away from the redesigned pages that name them. redesign-legacy
  # is the thin restyled shell those kept routes were built for.
  - scope:
      path: ""
      type: certifications
    values:
      layout: redesign-legacy
      author_profile: true
      share: true
  # _awards — same: listed on /certifications/, on the CV and on the home page.
  - scope:
      path: ""
      type: awards
    values:
      layout: redesign-legacy
      author_profile: true
      share: true
  # _news — ROUND 6. A milestone gets the same detail page a project gets:
  # the full-width title band, the prose column and the right margin column.
  # `view: home` is deliberate — News is a section of the home page, so the
  # navbar keeps marking About while the reader is inside a milestone.
  - scope:
      path: ""
      type: news
    values:
      layout: redesign-news
      view: home
      author_profile: true
  # _extra
  - scope:
      path: ""
      type: extra
    values:
      layout: redesign-legacy
      author_profile: true
      share: true


# Sass/SCSS
sass:
  sass_dir: _sass
  style: compressed # https://sass-lang.com/documentation/file.SASS_REFERENCE.html#output_style


# Outputting
permalink: /:categories/:title/
timezone: Etc/UTC # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

# Blog pagination (disabled for now - jekyll-paginate only works with index.html at root)
# To enable pagination later, consider using jekyll-paginate-v2
# paginate: 10
# paginate_path: "/blog/page:num/"


# Plugins
plugins:
  - jekyll-feed
  - jekyll-gist
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-redirect-from
  - jemoji

# Mimic GitHub Pages with --safe
whitelist:
  - jekyll-feed
  - jekyll-gist
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-redirect-from
  - jemoji


# Archives
#  Type
#  - GitHub Pages compatible archive pages built with Liquid ~> type: liquid (default)
#  - Jekyll Archives plugin archive pages ~> type: jekyll-archives
#  Path (examples)
#  - Archive page should exist at path when using Liquid method or you can
#    expect broken links (especially with breadcrumbs enabled)
#  - <base_path>/tags/my-awesome-tag/index.html ~> path: /tags/
#  - <base_path/categories/my-awesome-category/index.html ~> path: /categories/
#  - <base_path/my-awesome-category/index.html ~> path: /
category_archive:
  type: liquid
  path: /categories/
tag_archive:
  type: liquid
  path: /tags/
# https://github.com/jekyll/jekyll-archives
# jekyll-archives:
#   enabled:
#     - categories
#     - tags
#   layouts:
#     category: archive-taxonomy
#     tag: archive-taxonomy
#   permalinks:
#     category: /categories/:name/
#     tag: /tags/:name/


# HTML Compression
# - https://jch.penibelst.de/
compress_html:
  clippings: all
  ignore:
    envs: development
